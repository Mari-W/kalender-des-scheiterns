{% include 'header.twig' %}

{% for e in dates %}
    <p style="color: #cf2e78; size: 12px; margin-top: 20px; font-family: 'Roboto Slab', serif;">{{ e.date|date("dd.MM.yyyy") }}</p>
    <p style="font-family: 'Roboto Slab', serif; font-size: 16px">{% if e.type == "HISTORIC" %} HISTORISCH {% else %} PERSÖNLICH {% endif %}</p>
    <p class="text-break" style="font-size: 14px">{{ e.description }}</p>
    <p class="form-label-extra" style="font-size: 10px; margin-top:  -40px">- {{ e.name }}</p>
{% endfor %}

{% include 'footer.twig' %}
